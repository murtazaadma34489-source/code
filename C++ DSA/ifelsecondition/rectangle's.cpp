#include<iostream>

using namespace std;

int main (){
	
// find in there are Perimetre rectangles of  calculators are now 
	
	int widht= 34;// in there frist  are in variables 
	int lenght= 32;// in there Second are in variable 
	int area = widht * lenght;
    int Perimeter = 2*(lenght+widht);// in there are formular in rectangles of Perimetre of are now 
   cout<<"Area is rectangles :"<<Area<<endl;
   cout<<"Perimeter of rectangles:"<<Perimeter<<endl;
   int sum = does not is not exist is does not wroking jobs

	return 0;
}
